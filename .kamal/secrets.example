# Kamal runtime secrets — example only.
#
# This file is committed as a reference for what the deploy workflow expects.
# The real `.kamal/secrets` is written at runtime by GitHub Actions from
# GitHub Environment Secrets and is .gitignored.

KAMAL_REGISTRY_PASSWORD=

# Application secrets — set these in GitHub Environment Secrets.
DATABASE_URL=
NEXTAUTH_SECRET=
NEXTAUTH_URL=
